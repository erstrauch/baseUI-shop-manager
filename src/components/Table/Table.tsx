// const testColNames = ['Name', 'Total Price', 'Total Count', 'Price per Item'];

// const testColData = [
//     { name: "Blue beads", totalPrice: 25, totalCount: 15, PPI: (25/15)},
//     { name: "Glass hearts", totalPrice: 50, totalCount: 10, PPI: (50/10)},
// ];

// what if this table could take in the heading names dynamically? update this later, I think right now I will just add the table manually

function Table() {
  return <table />;
}

export default Table;
